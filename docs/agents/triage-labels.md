# Triage Labels

**Status: CURRENT** — this file is the live configuration for this repo. Read it as current guidance.

| Provenance | |
| --- | --- |
| Created | 2026-08-07 13:52 PDT (UTC-07:00) |
| Last updated | 2026-08-07 13:52 PDT (UTC-07:00) |
| Surface | Claude (Cowork) |
| Machine | `kriss-macbook-pro-local` |
| Project | `fabians-red-card` |
| Repository | [https://github.com/walksalot/fabians-red-card](https://github.com/walksalot/fabians-red-card) |
| Path | `docs/agents/triage-labels.md` |
| Branch @ commit | `main` @ [`fe6d299`](https://github.com/walksalot/fabians-red-card/commit/fe6d299) |
| Work summary | Mapped the five canonical triage roles to this repo's GitHub label strings for the `triage`, `to-tickets`, and `to-spec` skills; generated by the `setup-matt-pocock-skills` skill. |
| Session | [session_01HLGBtYxoyTUWMJoPyLk165](https://claude.ai/code/session_01HLGBtYxoyTUWMJoPyLk165) |
| Predecessor | None |
| Successor | None known |

The skills speak in terms of five canonical triage roles. This file maps those roles to the actual label strings used in this repo's issue tracker.

| Label in mattpocock/skills | Label in our tracker | Meaning                                  |
| -------------------------- | -------------------- | ---------------------------------------- |
| `needs-triage`             | `needs-triage`       | Maintainer needs to evaluate this issue  |
| `needs-info`               | `needs-info`         | Waiting on reporter for more information |
| `ready-for-agent`          | `ready-for-agent`    | Fully specified, ready for an AFK agent  |
| `ready-for-human`          | `ready-for-human`    | Requires human implementation            |
| `wontfix`                  | `wontfix`            | Will not be actioned                     |

When a skill mentions a role (e.g. "apply the AFK-ready triage label"), use the corresponding label string from this table.

Edit the right-hand column to match whatever vocabulary you actually use.

## Creating the labels

These label strings must exist in the GitHub repo before `/triage`, `/to-tickets`, or `/to-spec` can apply them. `wontfix` ships with every new GitHub repo; the other four usually do not. Create the missing ones once:

```sh
gh label create needs-triage    --color FBCA04 --description "Maintainer needs to evaluate this issue"  --force
gh label create needs-info      --color D4C5F9 --description "Waiting on reporter for more information" --force
gh label create ready-for-agent --color 0E8A16 --description "Fully specified, ready for an AFK agent"  --force
gh label create ready-for-human --color 1D76DB --description "Requires human implementation"            --force
gh label create wontfix         --color FFFFFF --description "Will not be actioned"                     --force
```
